.stats
  .stat-container
    .stat
      .stat-label Items Sold
      .stat-number
        = get_items_sold_count
  .stat-container
    .stat
      .stat-label Total Rev
      .stat-number
        = "$" + get_revenue.to_s
  .stat-container
    .stat
      .stat-label Average
      .stat-number
        = "$" + get_average_revenue.to_s + "/item"
%br
.stats
  - get_product_types.each do |product_type|
    -if get_items_sold_by_type_count(product_type) > 0
      .stat-container
        .stat
          .stat-label 
            =product_type
          .stat-number
            =get_items_sold_by_type_count(product_type)
.stats
  - get_product_types.each do |product_type|
    -if get_items_sold_by_type_count(product_type) > 0
      .stat-container
        .stat
          .stat-label 
            =product_type
          .stat-number
            = "$" + get_revenue_by_type(product_type).to_s 
.stats
  - get_product_types.each do |product_type|
    -if get_items_sold_by_type_count(product_type) > 0
      .stat-container
        .stat
          .stat-label 
            =product_type
          .stat-number
            = "$" + get_average_revenue_by_type(product_type).to_s